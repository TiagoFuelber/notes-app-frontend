import styled from "styled-components";

/**
 * @description makes a centered container with 1200px max-width on large screens
 */
const Container = styled.div`
  width: 1200px;
  max-width: 100%;
  padding: 0 15px;
  margin: 0 auto;
`;

export default Container;
